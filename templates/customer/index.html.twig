{% extends 'base.html.twig' %}

{% block body %}
<div class="container col-md-3 text-center mt-3">
    <h1 class="text text-center mt-4">Customer </h1>
    <a class="btn btn-outline-primary" href="{{ path('add_customer') }}">Add new customer</a>
    <table class="table table-responesive">
    <thead>
        <tr>
            <th>Customer ID</th>
            <th>Customer Name</th>
            <th>Customer Address</th>
            <th>Customer Phone</th>
            <th>Customer DateOfBirth</th>
            <th>Menu</th>
        </tr>
    </thead>

    <tbody>
    {% for customer in customers %}
       <tr>
                <td>{{ customer.id }}</td>
                <td>{{ customer.name }}</td>
                <td>
                <a class="btn btn-info" href="{{ path('view_custome_by_id', {'id': customer.id })}}">Detail</a>
                <a class="btn btn-warning" href="{{ path('edit_class', {'id': custoemr.id })}}">Edit</a>
                <a class="btn btn-danger" href="{{ path('delete_class', {'id': customer.id })}}"
                onclick = "return confirm('Do you want to delete this customer ? ');"
                >Delete</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
